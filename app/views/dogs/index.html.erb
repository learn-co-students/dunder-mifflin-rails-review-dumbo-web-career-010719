<h1>List of Dogs:</h1>
<ul>
  <% @dogs.each do |dog| %>
    <li><%= link_to dog.name, dog_path(dog) %> (<%= dog.employees.count %> owners)</li>
  <% end %>
</ul>

<div>
  <%= button_to "Back To Employees", employees_path, method: :get %>
</div>
